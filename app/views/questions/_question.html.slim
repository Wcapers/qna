= question.title
= question.body
= link_to 'Show', question_path(question)
- if current_user&.author_of?(question)
    = button_to  I18n.t('questions.destroy_button'), question_path(question), method: :delete
